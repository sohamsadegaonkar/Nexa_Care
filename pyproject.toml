[project]
name = "nexa-care"
version = "0.2.1"
description = "Privacy-first health-record platform with consent-scoped access."
requires-python = ">=3.12,<3.13"

[build-system]
requires = ["setuptools==70.2.0", "wheel==0.47.0"]
build-backend = "setuptools.build_meta"
